import config from "../config.firebase.json";

export const firebaseConfig = config as {
  apiKey: string;
  authDomain: string;
  projectId: string;
  storageBucket: string;
  messagingSenderId: string;
  appId: string;
};
